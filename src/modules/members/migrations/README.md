# Members module migrations

This folder contains Alembic revisions owned by the `members` module.

Run migrations through the host app configuration (demo startup or Alembic CLI)
so this module can be composed with other DB-backed modules via
`version_locations`.
